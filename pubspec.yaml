name: spacetomic
description: "Spacetomic"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  #UI
  device_preview: ^1.2.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  google_nav_bar: ^5.0.7

  # Bloc
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icon/

