import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B0C10); // deep space black
  static const Color surface = Color(0xFF1F2833);    // dark gray-blue

  // Primary
  static const Color primary = Color(0xFF8A2BE2);     // electric purple
  static const Color primaryVariant = Color(0xFF6A0DAD); // darker space purple

  // Secondary / Accent
  static const Color secondary = Color(0xFF00FFFF);   // neon cyan
  static const Color secondaryVariant = Color(0xFF76EEC6); // minty glow

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0BEC5); // muted gray-blue
  static const Color textFaded = Color(0xFF90A4AE);     // subtle gray

  // Status / Feedback
  static const Color success = Color(0xFF00E676); // bright green
  static const Color warning = Color(0xFFFFC107); // yellow
  static const Color error = Color(0xFFFF5252);   // soft red

  // Shadows / Glows
  static const Color glowPurple = Color(0xFF9C27B0);  // used in glow or neon effects
  static const Color glowBlue = Color(0xFF2196F3);
  static const Color glowPink = Color(0xFFE91E63);

  // Stars / Details
  static const Color starYellow = Color(0xFFFFD700);
  static const Color asteroidGray = Color(0xFF607D8B);
}
